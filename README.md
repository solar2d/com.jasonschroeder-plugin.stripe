Sources for the plugin `plugin.stripe`.

Add following to your `build.settings` to use:
```lua
{
    plugins = {
        "plugin.stripe" = {
            publisherId = "com.jasonschroeder",
        },
    },
}
```
